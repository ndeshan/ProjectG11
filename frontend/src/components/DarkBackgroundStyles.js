import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const DarkHeroBackground = styled(Box)(({ theme }) => ({
  backgroundImage: 'linear-gradient(rgba(18,18,18,0.8), rgba(30,30,30,0.8)), url(/images/1.jpg)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  backgroundAttachment: 'fixed',
  minHeight: '100vh',
  position: 'relative'
}));

export const DarkUniversityBackground = styled(Box)(({ theme }) => ({
  backgroundImage: 'linear-gradient(rgba(18,18,18,0.7), rgba(30,30,30,0.7)), url(/images/6.jpg)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  backgroundAttachment: 'fixed',
  minHeight: '100vh',
  position: 'relative'
}));

export const DarkCanteenBackground = styled(Box)(({ theme }) => ({
  backgroundImage: 'linear-gradient(rgba(18,18,18,0.7), rgba(30,30,30,0.7)), url(/images/3.jpg)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  backgroundAttachment: 'fixed',
  minHeight: '100vh',
  position: 'relative'
}));

export const DarkQueueBackground = styled(Box)(({ theme }) => ({
  backgroundImage: 'linear-gradient(rgba(18,18,18,0.7), rgba(30,30,30,0.7)), url(/images/8.jpg)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  backgroundAttachment: 'fixed',
  minHeight: '100vh',
  position: 'relative'
}));

export const DarkContactBackground = styled(Box)(({ theme }) => ({
  backgroundImage: 'linear-gradient(rgba(18,18,18,0.7), rgba(30,30,30,0.7)), url(/images/4.jpg)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  backgroundAttachment: 'fixed',
  minHeight: '100vh',
  position: 'relative'
}));

export const DarkReviewBackground = styled(Box)(({ theme }) => ({
  backgroundImage: 'linear-gradient(rgba(18,18,18,0.7), rgba(30,30,30,0.7)), url(/images/5.jpg)',
  backgroundSize: 'cover',
  backgroundPosition: 'center',
  backgroundAttachment: 'fixed',
  minHeight: '100vh',
  position: 'relative'
}));